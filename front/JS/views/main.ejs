<!DOCTYPE html>
<html lang="ko">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link rel="stylesheet" href="css/main.css">
    <title>Document</title>
</head>
<body>
    <div class="container">
        <h1>메모리얼 브금 맞추기</h1>
        <img class="img" src="/img/노노미.jpg">
        <div>
            <button class="button" onclick="goToURL()">시작</button>
        </div>
    </div>
    <script>
        function goToURL(){
            fetch('http://3.35.48.213:8080/shuffleMusicList',{
                method: 'GET',
                headers: {
                    'Content-Type': 'application/json',
                }
            })
            .then(response => {
                if(!response.ok){
                    throw new Error('http 오류: ' + response.status);
                }else{
                    console.log('섞기 성공');
                }
            })
            .catch(error => {
                console.error('에러 발생:', erorr);
            });
            window.location.href = '/quiz';
        }
    </script>
</body>
</html>